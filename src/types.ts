export declare class Product {
    code: string;
    name: string;
    description: string;
    shortName: string;
    group: string;
    style: string;
    versions: Array<string>;
    defaultVersion: string;
}