import main from "./main";
import provider from "./provider";
import src from "./src";
import plugins from "./plugins";

const ide = {
  main,
};

export default {
  ide,
  src,
  plugins,
  provider,
};
